####################################################################################################
# Licensed to the .NET Foundation under one or more agreements.  The .NET Foundation licenses this
# file to you under the MIT license.  See the LICENSE file in the project root for more information.
####################################################################################################

# Defines separate Windows and Linux stages that run ManualTests against Azure SQL Managed Instance
# using the packages produced by sqlclient-ci-package.

parameters:

  - name: buildConfiguration
    type: string
    values:
      - Debug
      - Release

  - name: debug
    type: boolean

  - name: dotnetVerbosity
    type: string
    values:
      - quiet
      - minimal
      - normal
      - detailed
      - diagnostic

  - name: netTestRuntimes
    type: object
    default: [net8.0, net9.0, net10.0]

  - name: netFrameworkTestRuntimes
    type: object
    default: [net462]

stages:

  - stage: windows
    displayName: Windows
    dependsOn: []
    jobs:

      # .NET Framework uses native SNI.
      - ${{ each runtime in parameters.netFrameworkTestRuntimes }}:
        - template: /eng/pipelines/ci/managed-instance/sqlclient-ci-managed-instance-job.yml@self
          parameters:
            buildConfiguration: ${{ parameters.buildConfiguration }}
            debug: ${{ parameters.debug }}
            dotnetVerbosity: ${{ parameters.dotnetVerbosity }}
            displayName: 'Win : Native SNI : ${{ runtime }}'
            jobNameSuffix: windows_native_sni_${{ replace(runtime, '.', '_') }}
            operatingSystem: Windows
            runtime: ${{ runtime }}
            useManagedSNI: false
            vmImage: ADO-Win25

      # .NET runs with both native and managed SNI.
      - ${{ each runtime in parameters.netTestRuntimes }}:
        - template: /eng/pipelines/ci/managed-instance/sqlclient-ci-managed-instance-job.yml@self
          parameters:
            buildConfiguration: ${{ parameters.buildConfiguration }}
            debug: ${{ parameters.debug }}
            displayName: 'Win : Native SNI : ${{ runtime }}'
            dotnetVerbosity: ${{ parameters.dotnetVerbosity }}
            jobNameSuffix: windows_native_sni_${{ replace(runtime, '.', '_') }}
            operatingSystem: Windows
            runtime: ${{ runtime }}
            useManagedSNI: false
            vmImage: ADO-Win25

        - template: /eng/pipelines/ci/managed-instance/sqlclient-ci-managed-instance-job.yml@self
          parameters:
            buildConfiguration: ${{ parameters.buildConfiguration }}
            debug: ${{ parameters.debug }}
            displayName: 'Win : Managed SNI : ${{ runtime }}'
            dotnetVerbosity: ${{ parameters.dotnetVerbosity }}
            jobNameSuffix: windows_managed_sni_${{ replace(runtime, '.', '_') }}
            operatingSystem: Windows
            runtime: ${{ runtime }}
            useManagedSNI: true
            vmImage: ADO-Win25

  - stage: linux
    displayName: Linux
    dependsOn: []
    jobs:

      # Managed SNI is always used on non-Windows platforms.
      - ${{ each runtime in parameters.netTestRuntimes }}:
        - template: /eng/pipelines/ci/managed-instance/sqlclient-ci-managed-instance-job.yml@self
          parameters:
            buildConfiguration: ${{ parameters.buildConfiguration }}
            debug: ${{ parameters.debug }}
            displayName: 'Linux : ${{ runtime }}'
            jobNameSuffix: linux_${{ replace(runtime, '.', '_') }}
            dotnetVerbosity: ${{ parameters.dotnetVerbosity }}
            operatingSystem: Linux
            runtime: ${{ runtime }}
            vmImage: ADO-UB24
