<docs>
  <members name="SqlColumnEncryptionEnclaveProvider">
    <SqlColumnEncryptionEnclaveProvider>
      <summary>
        The base class that defines the interface for enclave providers for Always Encrypted.
      </summary>
      <remarks>
        <para>
          An enclave is a protected region of memory inside SQL Server, used for computations on encrypted columns. An enclave provider encapsulates the client-side implementation details of the enclave attestation protocol as well as the logic for creating and caching enclave sessions.
        </para>
        <para>
          Attestation is a three-call sequence: <c>GetEnclaveSession</c>, <c>GetAttestationParameters</c>, then <c>CreateEnclaveSession</c>. The first call may take an attestation gate that is only released by the matching <c>CreateEnclaveSession</c> call.
        </para>
        <para>
          Callers must not mix the synchronous and asynchronous members within a single attestation sequence. The base implementation maintains separate gates for the two paths, so pairing <c>GetEnclaveSessionAsync</c> with the synchronous <c>CreateEnclaveSession</c> (or vice versa) leaves a gate held until its timeout expires and stalls other callers.
        </para>
      </remarks>
    </SqlColumnEncryptionEnclaveProvider>
    <ctor>
      <summary>
        Initializes a new instance of the <see cref="T:Microsoft.Data.SqlClient.SqlColumnEncryptionEnclaveProvider" /> class
      </summary>
    </ctor>
    <CreateEnclaveSession>
      <param name="enclaveAttestationInfo">
        The information the provider uses to attest the enclave and generate a symmetric key for the session. The format of this information is specific to the enclave attestation protocol.
      </param>
      <param name="clientDiffieHellmanKey">
        A Diffie-Hellman algorithm object that encapsulates a client-side key pair.
      </param>
      <param name="enclaveSessionParameters">
        The set of parameters required for an enclave session.
      </param>
      <param name="customData">
        The set of extra data needed for attesting the enclave.
      </param>
      <param name="customDataLength">
        The length of the extra data needed for attesting the enclave.
      </param>
      <param name="sqlEnclaveSession">
        The requested enclave session or <see langword="null" /> if the provider doesn't implement session caching.
      </param>
      <param name="counter">
        A counter that the enclave provider is expected to increment each time SqlClient retrieves the session from the cache. The purpose of this field is to prevent replay attacks.
      </param>
      <summary>
        When overridden in a derived class, performs enclave attestation, generates a symmetric key for the session, creates an enclave session and stores the session information in the cache.
      </summary>
    </CreateEnclaveSession>
    <CreateEnclaveSessionAsync>
      <param name="enclaveAttestationInfo">
        The information the provider uses to attest the enclave and generate a symmetric key for the session. The format of this information is specific to the enclave attestation protocol.
      </param>
      <param name="clientDiffieHellmanKey">
        A Diffie-Hellman algorithm object that encapsulates a client-side key pair.
      </param>
      <param name="enclaveSessionParameters">
        The set of parameters required for an enclave session.
      </param>
      <param name="customData">
        The set of extra data needed for attesting the enclave.
      </param>
      <param name="customDataLength">
        The length of the extra data needed for attesting the enclave.
      </param>
      <param name="cancellationToken">
        A token to cancel the asynchronous operation.
      </param>
      <summary>
        Asynchronously performs enclave attestation, generates a symmetric key for the session, creates an enclave session and stores the session information in the cache.
      </summary>
      <returns>
        A task that returns a tuple on completion. <c>SqlEnclaveSession</c> is the created enclave session, or <see langword="null" /> if the provider doesn't implement session caching. <c>Counter</c> is a counter that the enclave provider is expected to increment each time SqlClient retrieves the session from the cache, which prevents replay attacks.
      </returns>
      <remarks>
        <para>
          Asynchronous methods cannot declare <see langword="out" /> parameters, so the values reported through the <see langword="out" /> parameters of <c>CreateEnclaveSession</c> are returned as a tuple.
        </para>
        <para>
          The default implementation first checks the <paramref name="cancellationToken" /> and returns a canceled task if cancellation has been requested. Otherwise, it calls the synchronous <c>CreateEnclaveSession</c> method and wraps the result in a completed task. If the synchronous method throws, the exception is caught and a faulted task is returned rather than throwing synchronously.
        </para>
        <para>
          Providers that perform network I/O during attestation must override this method with a genuinely asynchronous implementation so that the calling thread is not blocked for the duration of the attestation round trip.
        </para>
      </remarks>
    </CreateEnclaveSessionAsync>
    <GetAttestationParameters>
      <param name="attestationUrl">
        The endpoint of an attestation service for attesting the enclave.
      </param>
      <param name="customData">
        A set of extra data needed for attesting the enclave.
      </param>
      <param name="customDataLength">
        The length of the extra data needed for attesting the enclave.
      </param>
      <summary>
        Gets the information that SqlClient subsequently uses to initiate the process of attesting the enclave and to establish a secure session with the enclave.
      </summary>
      <returns>
        The information SqlClient subsequently uses to initiate the process of attesting the enclave and to establish a secure session with the enclave.
      </returns>
    </GetAttestationParameters>
    <GetAttestationParametersAsync>
      <param name="attestationUrl">
        The endpoint of an attestation service for attesting the enclave.
      </param>
      <param name="customData">
        A set of extra data needed for attesting the enclave.
      </param>
      <param name="customDataLength">
        The length of the extra data needed for attesting the enclave.
      </param>
      <param name="cancellationToken">
        A token to cancel the asynchronous operation.
      </param>
      <summary>
        Asynchronously gets the information that SqlClient subsequently uses to initiate the process of attesting the enclave and to establish a secure session with the enclave.
      </summary>
      <returns>
        A task that returns, on completion, the information SqlClient subsequently uses to initiate the process of attesting the enclave and to establish a secure session with the enclave.
      </returns>
      <remarks>
        <para>
          The default implementation first checks the <paramref name="cancellationToken" /> and returns a canceled task if cancellation has been requested. Otherwise, it calls the synchronous <c>GetAttestationParameters</c> method and wraps the result in a completed task. If the synchronous method throws, the exception is caught and a faulted task is returned rather than throwing synchronously.
        </para>
        <para>
          This operation is CPU bound for all in-box providers, so overriding it is only necessary if a provider performs I/O here.
        </para>
      </remarks>
    </GetAttestationParametersAsync>
    <GetEnclaveSession>
      <param name="enclaveSessionParameters">
        The set of parameters required for enclave session.
      </param>
      <param name="generateCustomData">
        <see langword="true" /> to indicate that a set of extra data needs to be generated for attestation; otherwise, <see langword="false" />.
      </param>
      <param name="isRetry">
        Indicates if this is a retry from a failed call.
      </param>
      <param name="sqlEnclaveSession">
        When this method returns, the requested enclave session or <see langword="null" /> if the provider doesn't implement session caching. This parameter is treated as uninitialized.
      </param>
      <param name="counter">
        A counter that the enclave provider is expected to increment each time SqlClient retrieves the session from the cache. The purpose of this field is to prevent replay attacks.
      </param>
      <param name="customData">
        A set of extra data needed for attesting the enclave.
      </param>
      <param name="customDataLength">
        The length of the extra data needed for attesting the enclave.
      </param>
      <summary>
        When overridden in a derived class, looks up an existing enclave session information in the enclave session cache. If the enclave provider doesn't implement enclave session caching, this method is expected to return <see langword="null" /> in the <paramref name="sqlEnclaveSession" /> parameter.
      </summary>
    </GetEnclaveSession>
    <GetEnclaveSessionAsync>
      <param name="enclaveSessionParameters">
        The set of parameters required for enclave session.
      </param>
      <param name="generateCustomData">
        <see langword="true" /> to indicate that a set of extra data needs to be generated for attestation; otherwise, <see langword="false" />.
      </param>
      <param name="isRetry">
        Indicates if this is a retry from a failed call.
      </param>
      <param name="cancellationToken">
        A token to cancel the asynchronous operation.
      </param>
      <summary>
        Asynchronously looks up an existing enclave session information in the enclave session cache. If the enclave provider doesn't implement enclave session caching, this method is expected to return <see langword="null" /> for the enclave session.
      </summary>
      <returns>
        A task that returns a tuple on completion. <c>SqlEnclaveSession</c> is the requested enclave session, or <see langword="null" /> if the provider doesn't implement session caching. <c>Counter</c> is a counter that the enclave provider is expected to increment each time SqlClient retrieves the session from the cache, which prevents replay attacks. <c>CustomData</c> is a set of extra data needed for attesting the enclave, and <c>CustomDataLength</c> is its length.
      </returns>
      <remarks>
        <para>
          Asynchronous methods cannot declare <see langword="out" /> parameters, so the values reported through the <see langword="out" /> parameters of <c>GetEnclaveSession</c> are returned as a tuple.
        </para>
        <para>
          The default implementation first checks the <paramref name="cancellationToken" /> and returns a canceled task if cancellation has been requested. Otherwise, it calls the synchronous <c>GetEnclaveSession</c> method and wraps the result in a completed task. If the synchronous method throws, the exception is caught and a faulted task is returned rather than throwing synchronously.
        </para>
        <para>
          Providers that perform network I/O during attestation must override this method with a genuinely asynchronous implementation so that the calling thread is not blocked while waiting for an in-flight attestation to complete.
        </para>
      </remarks>
    </GetEnclaveSessionAsync>
    <InvalidateEnclaveSession>
      <param name="enclaveSessionParameters">
        The set of parameters required for enclave session.
      </param>
      <param name="enclaveSession">
        The session to be invalidated.
      </param>
      <summary>
        When overridden in a derived class, looks up and evicts an enclave session from the enclave session cache, if the provider implements session caching.
      </summary>
    </InvalidateEnclaveSession>
    <InvalidateEnclaveSessionAsync>
      <param name="enclaveSessionParameters">
        The set of parameters required for enclave session.
      </param>
      <param name="enclaveSession">
        The session to be invalidated.
      </param>
      <param name="cancellationToken">
        A token to cancel the asynchronous operation.
      </param>
      <summary>
        Asynchronously looks up and evicts an enclave session from the enclave session cache, if the provider implements session caching.
      </summary>
      <returns>
        A task that completes when the enclave session has been invalidated.
      </returns>
      <remarks>
        <para>
          The default implementation first checks the <paramref name="cancellationToken" /> and returns a canceled task if cancellation has been requested. Otherwise, it calls the synchronous <c>InvalidateEnclaveSession</c> method and returns a completed task. If the synchronous method throws, the exception is caught and a faulted task is returned rather than throwing synchronously.
        </para>
        <para>
          Session invalidation is an in-memory cache operation for all in-box providers.
        </para>
      </remarks>
    </InvalidateEnclaveSessionAsync>
  </members>
</docs>
